# Spark 3.5 实际使用 log4j2（log4j-slf4j2-impl + log4j-core）
# 放在 classpath 上会被 log4j2 自动发现，从而覆盖 Spark 内置的
# log4j2-defaults.properties。
#
# 学习时把 rootLogger.level 调成 INFO / DEBUG 可以看到：
#   - DAGScheduler 的 stage 切分过程
#   - TaskSetManager 的调度与重试
#   - Shuffle 的读写细节

rootLogger.level = WARN
rootLogger.appenderRef.console.ref = console

appender.console.name = console
appender.console.type = CONSOLE
appender.console.target = SYSTEM_ERR
appender.console.layout.type = PatternLayout
appender.console.layout.pattern = %d{yy/MM/dd HH:mm:ss} %p %c{1}: %m%n

# 只想看 Spark 自己的日志时，把 root 调成 ERROR、下面这行调成 INFO
logger.spark.name = org.apache.spark
logger.spark.level = WARN

logger.sparkproject.name = org.sparkproject
logger.sparkproject.level = WARN

# 屏蔽过于啰嗦的第三方组件
logger.hadoop.name = org.apache.hadoop
logger.hadoop.level = WARN
logger.parquet.name = org.apache.parquet
logger.parquet.level = ERROR
logger.jetty.name = org.eclipse.jetty
logger.jetty.level = WARN
